const { getUrl } = require('../db/mysqlQueries');
const mysqlQueries = require ('../db/mysqlQueries');

function urlShortener () {

    const getUrl = (alias) => {
    };

    const saveUrl = (alias, url) => {};

    return {getUrl, saveUrl};
}

module.exports = urlShortener ();